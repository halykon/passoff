{
  "name": "passoff",
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@undefined/lint": "github:JohnDeved/undefined-lint"
  },
  "scripts": {
    "build": "npm run build --prefix",
    "postinstall": "npm i --prefix pwa && npm i --prefix storybook"
  }
}
